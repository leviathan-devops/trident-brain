# TRIDENT CODE REVIEW MODE v3.2

Self-contained OpenCode plugin for algorithmic code review.

---

## QUICK START

```bash
# Build self-contained bundle
./scripts/build.sh

# Deploy to opencode plugins
./scripts/deploy.sh
```

---

## TOOLS

| Tool | Command |
|------|---------|
| `trident-audit [target]` | Run code audit, generates artifact |
| `trident-status` | Show audit state |
| `trident-report` | Full findings report |
| `trident-help` | Available commands |

---

## USAGE

```bash
opencode --agent trident
# Say: "audit this /path/to/project"
```

---

## FILES

```
├── src/
│   ├── index.ts              # Plugin entry + tools
│   ├── algorithmic-core.ts   # Pattern detection engine
│   └── artifact-writer.ts  # Artifact generation
├── dist/
│   └── index.js              # Self-contained bundle (530KB)
├── scripts/
│   ├── build.sh              # Build with bun --bundle
│   └── deploy.sh             # Deploy + verify opencode.json
└── docs/
    └── SHIP_READY.md         # Test results
```

---

## CORE PRINCIPLE

> **"Trident Documents. Humans Fix."**

---

*Generated by Trident Brain v3.2*